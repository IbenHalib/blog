{{ category.name }}

<ul>
    {% for article in articles %}
        <li>
            <a href="{{ path('_see', {'id': article.id}) }}">
                {{ article.title }}
            </a>
        </li>

    {% endfor %}
</ul>