{% extends 'VadimGuestBundle::layout.html.twig' %}
{% block page_content %}
<div>
{{ form(form) }}
</div>

    {#{{ render('VadimGuestBundle:Default:posts.html.twig', {'posts': posts})}} }}#}
    {{ render(controller('VadimGuestBundle:Default:posts', { 'page' : 1})) }}

        <input type="button" id="but1" value="Show More" />
        {#{{ form(formSearch) }}#}


{% endblock  page_content %}